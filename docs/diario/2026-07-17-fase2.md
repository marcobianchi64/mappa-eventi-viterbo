# Diario — Fase 2 (fonti AUTO-2)

**Data:** 2026-07-17

## Implementato

- Connettore HTML **ViterboToday** (`html-viterbotoday`) — listing CityNews `/eventi/`
- Connettore HTML **TusciaUp** (`html-tusciaup`) — parser generico listing
- `parseEventDateHints()` per date da slug/testo (intervalli e giorni singoli)
- `fetch-html.ts` con User-Agent browser (CityNews blocca bot generici)
- Collector carica `packages/collector/.env` via **dotenv** (`npm run collect` senza `source` manuale)
- Seed + migration `003_fase2_auto_sources.sql` — modalità `automatic` per le due fonti

## Fonti automatiche totali (5)

1. Tuscia Eventi (RSS)
2. Eventi della Tuscia (RSS)
3. Comune di Viterbo (HTML)
4. ViterboToday (HTML)
5. TusciaUp (HTML)

## Azione utente

```bash
git pull
npm ci
npm run collect
```

Opzionale su Supabase SQL Editor: `supabase/migrations/003_fase2_auto_sources.sql`
