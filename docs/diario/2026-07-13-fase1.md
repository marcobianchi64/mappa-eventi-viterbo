# Diario — 2026-07-13 — Fase 1 motore raccolta

## Fatto

- Package `@atlas/collector` con connettori RSS (Tuscia Eventi, Eventi della Tuscia) e HTML (Comune Viterbo).
- Normalizzazione date italiane, categorie, geocoding Nominatim con cache.
- Writer Supabase con dedup, auto-pubblicazione fonti A/B, log `source_sync_logs`.
- CLI: `npm run collect` + GitHub Actions giornaliero.
- Test live RSS: 5 eventi Tuscia Eventi, 25 Eventi della Tuscia.

## Azione richiesta

1. Eseguire `supabase/migrations/002_collector.sql`
2. Aggiungere secret `SUPABASE_SERVICE_ROLE_KEY` su GitHub
3. Eseguire `npm run collect` in locale per primo sync

## Nota Comune Viterbo

Il sito può bloccare alcune reti; il connettore HTML è pronto. Se fallisce da cloud, funziona da rete locale o con retry.
