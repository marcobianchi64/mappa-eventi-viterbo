# Diario — 2026-07-13 — Fase 0 tecnica avviata

## Fatto

- Basato su monorepo modulare (`apps/web`, `apps/admin`, `packages/*`).
- Schema SQL: territori, fonti, segnalazioni (`supabase/migrations/001_*`).
- Seed pilota Viterbo con 6 fonti (`supabase/seed_viterbo.sql`).
- API layer esteso in `@atlas/supabase-client`.
- 6 categorie macro (Musica, Cultura, Enogastronomia, Sport, Famiglie, Altri).
- Web: segnalazione con contatto, dedup client-side, `event.html` per condivisione.
- Admin: dashboard, fonti, eventi pending, segnalazioni utenti.
- Build e typecheck OK.

## Azione richiesta (Supabase)

Eseguire in ordine su SQL Editor:
1. `supabase/schema.sql`
2. `supabase/migrations/001_fase0_territories_sources.sql`
3. `supabase/seed_viterbo.sql`

## Prossimo

- Fase 1: motore raccolta automatica (Comune Viterbo + Tuscia Eventi).
