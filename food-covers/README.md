# Le tue foto enogastronomia

Metti qui **7 immagini** con questi nomi:

- `foto1` (jpg, png o webp)
- `foto2`
- `foto3`
- `foto4`
- `foto5`
- `foto6`
- `foto7`

Poi dalla root del progetto (dove c’è `package.json`):

```bash
npm run prepare:food-covers
npm run dev
```

Nel browser: **Ctrl+Shift+R**

> Questa cartella è nella **root del progetto**, accanto a `package.json`.  
> Non usare `apps/web/public/covers/food/` a mano.
